# 前端环境变量模板（复制为 .env.local 或 .env.development 使用）
# Google Identity Services OAuth Client ID（Web 类型）。
# 留空时登录页隐藏「使用 Google 登录」按钮，不影响其他功能。
# 获取方式：Google Cloud Console → APIs & Services → Credentials → 创建 OAuth Client ID (Web)
VITE_GOOGLE_CLIENT_ID=