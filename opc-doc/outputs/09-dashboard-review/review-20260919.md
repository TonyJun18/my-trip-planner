# 经营复盘 · my-trip-planner（2026-09-19）

## 经营状态摘要

**项目**：my-trip-planner（FastAPI + LangGraph + Vue3 多 Agent 智能行程规划）

**完成度（已核查）**：
- 建盘期产物完整：4 Agent 协作（AttractionSearchAgent / WeatherQueryAgent / HotelAgent / PlannerAgent），采集并行（asyncio.gather）+ 编排层补餐饮 + Planner 整合
- LLM 输出 Pydantic 强校验 + 失败自纠正（最多 3 次）
- 统一重试（指数退避）+ 进程内熔断器
- 异步规划任务（task_id + 轮询 + T-A-O 轨迹）
- JWT 用户体系（邮箱/手机号注册登录，PBKDF2）
- 预算按站点真实费用代码校准
- 测试 24/24 通过（FakeLLM 测试替身，不耗 token 不依赖网络）

**数据真实度**：高德 POI 真实景点/酒店/天气；未配 key 降级 Tavily / wttr.in。
局限：酒店价格恒为 0（高德免费接口不返回价格），预算中 hotel 项为空壳。

**运营状态**：无真实用户数据。定位未定（个人 showcase / 公司 PoC / 独立产品），
是当前一切发散的总源头。

## 瓶颈判断（三选一确认）

| 假设 | 内容 | 代价 |
|------|------|------|
| A 需求验证不足 | 定位没立住，无真实用户，产品或被打败 | 继续加功能可能是在验证没人要的需求 |
| B 数据深度不够 | 酒店价格 0、评分缺失、无预订闭环，体验泛泛 | 补数据有成本与合规问题，竞品可抄 |
| **C 资产沉淀不足（已确认）** | **一套多 Agent 编排模式只活在一个 repo 里，未对外沉淀与展示** | **止步 demo，价值只体现在本地，不复用即成沉没成本** |

**用户已确认核心瓶颈：C 资产沉淀不足。**

选择依据：该项目的最真实价值是"把多 Agent 编排做成生产级工程的能力证据"，
行程规划只是最合适的载体。C 做法的同时，A（需求验证）作为"对外展示"的自然
副产物被验证——有人看了愿意用，再转产品。

## 下一周期唯一优先重点

**内容沉淀**：打磨 README + 写 2-3 篇多 Agent 工程实践深度文章（掘金/知乎/公众号），
把编排模式讲透（单一职责 Agent 提示词、并行采集、Pydantic 强校验自纠正、熔断重试、
T-A-O 轨迹），对简历和个人品牌直接生效；A 验证作为展示副产物，不单独立线。

**顺延（不占本周期重点）**：开源脚手架抽象、上线 Demo 部署。

## 候选文章主题（备忘）

1. 多 Agent 编排不是堆 Agent：单一职责 + 并行采集 + 编排层补位
2. LLM 输出强校验与自纠正——如何让 Agent 稳定输出结构化 JSON
3. Agent 的容错工程：重试、熔断、降级，以及 FakeLLM 测试替身